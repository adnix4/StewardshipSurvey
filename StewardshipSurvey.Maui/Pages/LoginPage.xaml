<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StewardshipSurvey.Maui.LoginPage"
             Title="Login"
             BackgroundColor="{StaticResource PageBackgroundColor}">

    <VerticalStackLayout Padding="20" Spacing="15" VerticalOptions="Center" HorizontalOptions="Center">
        <!-- Logo/Title -->
        <StackLayout HorizontalOptions="Center" VerticalOptions="Start" Spacing="10" Margin="0,0,0,40">
            <Label Text="St. Mark" FontSize="32" FontAttributes="Bold" HorizontalTextAlignment="Center" />
            <Label Text="Stewardship Survey" FontSize="18" HorizontalTextAlignment="Center" TextColor="{StaticResource TextSecondaryColor}" />
        </StackLayout>

        <!-- Email Entry -->
        <StackLayout Spacing="5">
            <Label Text="Email" FontAttributes="Bold" />
            <Entry 
                x:Name="EmailEntry"
                Placeholder="Enter your email"
                Keyboard="Email"
                Text="{Binding Email}" 
                PlaceholderColor="#999999" />
        </StackLayout>

        <!-- Password Entry -->
        <StackLayout Spacing="5">
            <Label Text="Password" FontAttributes="Bold" />
            <Entry 
                x:Name="PasswordEntry"
                Placeholder="Enter your password"
                IsPassword="True"
                Text="{Binding Password}" 
                PlaceholderColor="#999999" />
        </StackLayout>

        <!-- Error Message -->
        <Label 
            Text="{Binding ErrorMessage}" 
            TextColor="{StaticResource ErrorColor}"
            IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"
            FontSize="12"
            Margin="0,10" />

        <!-- Login Button -->
        <Button 
            Text="Login"
            Command="{Binding LoginCommand}"
            IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
            Margin="0,10" />

        <!-- Loading Indicator -->
        <ActivityIndicator 
            IsRunning="{Binding IsLoading}"
            IsVisible="{Binding IsLoading}"
            Margin="0,20" />
    </VerticalStackLayout>
</ContentPage>
